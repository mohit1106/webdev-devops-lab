import Image from "next/image";
// in app dir, this page.tsx is inside users, so we can just directly open localhost:3000/users 
export default function Home() {
  return (
    <div>
      hi users page 
    </div>
  );
}
