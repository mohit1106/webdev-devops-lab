npm i -g pnpm
npx create-turbo@latest

pnpm install
pnpm run dev

inside backend folders in apps:
    npm init -y        
    import typescript config from packages in both backend apps
    add the same package as dependency in packages.json of backend apps
    pnpm install


add build, dev and start scripts in backend servers packages.json:
"scripts": {
    "build": "tsc -b",
    "start": "node dist/index.js",
    "dev": "npm run build && npm run start"
},


"compilerOptions": {
    "rootDir": "./src",
    "outDir": "./dist"
}